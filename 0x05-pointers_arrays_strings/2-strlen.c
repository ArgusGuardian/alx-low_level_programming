#include "main.h"
/**
 * _strlen - function to swap 2 integers
 * @s: pointer to a string
 * Return: returns the lenght of a string
 */
int _strlen(char *s)
{
	int count;

	count = 0;
	while (s != NULL)
		count++;

	return (count);
}
