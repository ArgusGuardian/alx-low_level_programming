#include "lists.h"
/**
 *listint_len - return the number of nodes in linked list
 *@h: linked list
 *Return: number of nodes
 *
 */
listint_t *add_nodeint(listint_t **head, const int n)
{
	listint_t *newnode;

	newnode = (listint_t *)calloc(sizeof(listint_t));
}
